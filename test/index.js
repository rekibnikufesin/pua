var expect = require('chai').expect;

var pkg = require('./package.json');

// a passing test
describe('I really should', function(){
  it('add some legitimate tests!', function(){
    expect(true).to.equal(true);
  })
});
